[core]
    pager = lv -c
[color]
    ui          = auto
    status      = auto
    diff        = auto
    branch      = auto
    interactive = auto
    grep        = auto
[alias]
    co = checkout
    ci = commit -a
    st = status
    up = pull --rebase
